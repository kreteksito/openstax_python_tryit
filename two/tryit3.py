"""After the point

The program print concatenation of strings as a number float but 'string' Ex: 2.3"""

num = 2
after_the_point = str(input("Enter a number for the decimal position after the point: "))

#print concatenation of strings
print(str(num) + "." + after)