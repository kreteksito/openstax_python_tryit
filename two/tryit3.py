"""After the point

The program print concatenation of strings as a number float but 'string' Ex: 2.3"""

num = 2
decimal = str(input("Enter a number for the decimal position after the point: "))

#print concatenation of strings
print(str(num) + "." + decimal)